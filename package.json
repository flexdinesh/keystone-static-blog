{
  "name": "keystone-static-blog",
  "version": "0.0.1",
  "description": "Starter template to statically generate blogs with Keystone and Next.js",
  "repository": "git@github.com:flexdinesh/keystone-static-blog.git",
  "author": "Dinesh Pandiyan <flexdinesh@gmail.com>",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "next-app",
    "keystone-server"
  ],
  "scripts": {
    "dev": "concurrently --names \"SERVER,APP\" -c \"magenta.bold,green.bold\" \"cd keystone-server && yarn dev\" \"cd next-app && yarn dev\"",
    "lint": "eslint . --ext .ts --ext .tsx",
    "lint:fix": "eslint . --ext .ts --ext .tsx --fix",
    "start:keystone": "cd keystone-server && yarn build && yarn start",
    "webhint": "npx hint http://localhost:3000",
    "export:app": "cd next-app && yarn clean && yarn export",
    "kill:server": "kill-port --port 3000",
    "x": "concurrently -s first \"yarn start:keystone\" \"wait-on http://localhost:3000 && yarn kill:server\"",
    "y": "node scripts/export.js",
    "export": "concurrently --names \"KEYSTONE,NEXT-BUILD\" --success \"last\" \"yarn start:keystone\"  \"wait-on http://localhost:3000 && yarn export:app && yarn kill:server\"",
    "push": "git add. && git commit -m 'New build 🚀' && git push origin head"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^5.33.1",
    "@typescript-eslint/parser": "^5.33.1",
    "concurrently": "^7.3.0",
    "eslint": "^8.22.0",
    "eslint-config-prettier": "^8.5.0",
    "eslint-plugin-prettier": "^4.2.1",
    "eslint-plugin-react": "^7.30.1",
    "eslint-plugin-react-hooks": "^4.6.0",
    "kill-port": "^2.0.1",
    "prettier": "^2.7.1",
    "wait-on": "^6.0.1"
  }
}
